# ---------------------------------------------------------------------------
# DeepGold RL — Python 3.11 dependencies
# ---------------------------------------------------------------------------
# Create and activate a virtual environment first:
#   python -m venv .venv
#   .venv\Scripts\activate            (Windows PowerShell: .venv\Scripts\Activate.ps1)
#   pip install --upgrade pip
#
# GPU PyTorch — install the CUDA build BEFORE the rest so pip does not pull the
# CPU-only wheel. Pick the index matching your NVIDIA driver (cu128 works on
# recent drivers; check `nvidia-smi`). Works on any CUDA GPU (GTX 1650 Ti,
# RTX 3070, etc.):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# Then:
#   pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Core RL stack
gymnasium>=0.29.1,<1.1.0
stable-baselines3>=2.3.0,<3.0.0
torch>=2.2.0                      # install the CUDA build separately (see above)

# Data / numerics
numpy>=1.26,<2.2
pandas>=2.1
scikit-learn>=1.3                 # scalers for feature normalization
joblib>=1.3                       # persisting the fitted scaler

# Visualization & logging
matplotlib>=3.8
tensorboard>=2.15
tqdm>=4.66                        # progress bars during training
rich>=13.0                       # pretty SB3 training progress bar

# Configuration
PyYAML>=6.0

# Validation & research (V2 / Phase 3)
optuna>=3.4                       # hyperparameter tuning (validation-set objective)

# Real historical data (free, no broker account) — used by scripts/download_dukascopy.py
dukascopy-python>=4.0            # downloads real XAUUSD OHLCV from Dukascopy

# Notebook / IDE support (Jupyter + VSCode)
jupyter>=1.0
ipykernel>=6.29

# Live trading (Windows only; safe to skip on Linux/macOS)
MetaTrader5>=5.0.45 ; platform_system == "Windows"
