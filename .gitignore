# Virtual environments
.venv/
venv/
env/.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Project artefacts (regenerated at runtime)
models/*.zip
models/**/*.zip
models/*.pkl
models/*.joblib
models/*.json
models/best_model/
models/checkpoints/
logs/tensorboard/
logs/backtest/
logs/eval/
logs/walk_forward/
logs/multi_dataset/
logs/arch_comparison/
logs/*.log
models/*_best_params.json

# Data — keep the example, ignore large real exports
data/*.csv
!data/XAUUSD_M5_example.csv
data/.dukascopy_cache/
logs/duka_download.log

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
!.vscode/settings.json
*.log

# Claude Code (machine-local config + permissions cache)
.claude/
# (CLAUDE.md at the repo root IS tracked — it travels with the project)

# Grid evaluation outputs — TRACKED (cells.csv/summary.json are tiny scientific
# artifacts used by compare_grids.py to baseline new runs against past results)

# Background task / temporary outputs
bg_task_logs/
tasks/
